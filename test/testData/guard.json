{
    "let f = (x, y) -> +(x, y), >(x, 0)\n| -(x, y);\nf(1, 2);": 3,
    "let f = (x, y) -> +(x, y), >(x, 0) | -(x, y);f(-1, 2);": -3,
    "let f = (x, y) -> +(x, y), <(x, 0) | -(x, y), <(x, 10) | *(x, y), <(x, 20);f(16, 2);": 32
}
